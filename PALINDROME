#include <stdio.h>
#include<string.h>
void main() {
    char a[20];
    printf("Enter a name:");
    scanf("%s",a);
    int len=strlen(a);
    for(int i=0,j=len-1;i<=j;i++,j--){
        if (a[i]!=a[j]){
        printf("not palindrome");
        return ;
        }
    }
printf("Palindrome");
}
