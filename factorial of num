#include<stdio.h>
#include<conio.h>
int fact(int);
int fact(int a)
{
    if (a==1)
        return;
    else
        return a*fact(a-1);
}
void main()
{
    int a;
    printf("Enter a number:");
    scanf("%d",&a);
    printf("Factorial is %d",fact(a));
}
